<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <title>Dixit</title>

    <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
    <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
    <%= csrf_meta_tags %>

    <!-- HTML5 shim and Respond.js IE8 support of HTML5 elements and media queries -->
    <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js"></script>
      <script src="https://oss.maxcdn.com/libs/respond.js/1.4.2/respond.min.js"></script>
    <![endif]-->
  </head>
  <body>
    <div class="container">
      <div class="row">
        <div class="page-header">
          <h1>StoryLine <small>Create imaginative stories with images</small></h1>
          <div class="user-button">
            <button type="button" class="btn btn-link dropdown-toggle" data-toggle="dropdown">
              Welcome Jeroen van Baarsen <span class="caret"></span>
            </button>
            <ul class="dropdown-menu" role="menu">
              <li><a href="#">Friends</a></li>
              <li class="divider"></li>
              <li><%= link_to "Sign out", sign_out_path, method: :delete %></li>
            </ul>
          </div>
        </div>
      </div>
      <div class="row">
        <div class="col-md-6">
          <h1>Your stories in progress</h1>
          <%= render "games/story", finished: false %>
          <%= render "games/story", finished: false %>
          <%= render "games/story", finished: false %>
          <%= render "games/story", finished: false %>
        </div>
        <div class="col-md-6">
          <h1>Finished stories</h1>
          <%= render "games/story", finished: true %>
          <%= render "games/story", finished: true %>
          <%= render "games/story", finished: true %>
          <%= render "games/story", finished: true %>
        </div>
      </div>
      <%= yield %>

      <% if flash[:error] -%>
        <p class='error'><%=h flash[:error] %></p>
      <% end -%>
  </body>
</html>
